# Google Drive Index - Environment Configuration
# Copy this file to .env or configure in Cloudflare Workers dashboard

# ===========================================
# GOOGLE OAUTH CREDENTIALS (Required)
# ===========================================
# Get these from Google Cloud Console: https://console.cloud.google.com/
CLIENT_ID=your_google_client_id_here
CLIENT_SECRET=your_google_client_secret_here
REFRESH_TOKEN=your_refresh_token_here

# ===========================================
# ENCRYPTION KEYS (Required - Generate your own!)
# ===========================================
# Generate a 256-bit (32 byte) hex key for AES encryption
# You can generate one using: openssl rand -hex 32
CRYPTO_BASE_KEY=your_256_bit_hex_key_here

# Generate a 256-bit (32 byte) hex key for HMAC
HMAC_BASE_KEY=your_256_bit_hex_key_here

# Generate a 128-bit (16 byte) IV as JSON array
# You can generate one using: node -e "console.log(JSON.stringify([...crypto.getRandomValues(new Uint8Array(16))]))"
ENCRYPT_IV=[247, 254, 106, 195, 32, 148, 131, 244, 222, 133, 26, 182, 20, 138, 215, 81]

# ===========================================
# DRIVE CONFIGURATION
# ===========================================
# Root drive ID - use "root" for My Drive or Shared Drive ID
ROOT_DRIVE_ID=root

# ===========================================
# OPTIONAL: LOGIN SYSTEM
# ===========================================
# Set to true to enable login
ENABLE_LOGIN=false

# Google OAuth for login (if using social login)
GOOGLE_CLIENT_ID_FOR_LOGIN=
GOOGLE_CLIENT_SECRET_FOR_LOGIN=
REDIRECT_DOMAIN=https://your-worker.your-subdomain.workers.dev

# ===========================================
# OPTIONAL: SERVICE ACCOUNT
# ===========================================
# Set to true if using service account instead of OAuth
USE_SERVICE_ACCOUNT=false
# Place service account JSON in temp/sa.json (DO NOT COMMIT!)

# ===========================================
# CLOUDFLARE KV (if using KV for user storage)
# ===========================================
# Configure in wrangler.toml or Cloudflare dashboard
# KV_NAMESPACE_ID=your_kv_namespace_id

# ===========================================
# SECURITY SETTINGS
# ===========================================
# Comma-separated list of blocked regions (ISO country codes)
BLOCKED_REGIONS=
# Comma-separated list of blocked ASN numbers
BLOCKED_ASN=
